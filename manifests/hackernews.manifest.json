{
  "manifest_version": 1,
  "slug": "hackernews",
  "name": "Hacker News",
  "description": "Crawls Hacker News stories and comments",
  "type": "hackernews",
  "api_type": "api",
  "crawler_type": "search",
  "file": "crawlers/hackernews.ts",
  "has_discover": true,
  "options_schema": {
    "description": "HackerNews crawler options - each crawler tracks ONE content type",
    "$id": "HackerNewsOptions",
    "type": "object",
    "required": [
      "search_query",
      "content_type"
    ],
    "properties": {
      "search_query": {
        "description": "Search term to find HN stories and comments (brand/product name)",
        "minLength": 1,
        "type": "string"
      },
      "content_type": {
        "description": "Content type: ONE type per crawler for reliable checkpointing. Create separate crawlers for different types.",
        "anyOf": [
          {
            "const": "story",
            "type": "string"
          },
          {
            "const": "comment",
            "type": "string"
          },
          {
            "const": "ask_hn",
            "type": "string"
          },
          {
            "const": "show_hn",
            "type": "string"
          }
        ]
      },
      "story_ids": {
        "description": "Track specific story IDs (optional, only used with story content types)",
        "minItems": 1,
        "type": "array",
        "items": {
          "type": "number"
        }
      },
      "lookback_days": {
        "description": "Number of days to look back for historical data. Default: 365 (1 year). Maximum: 730 (2 years).",
        "minimum": 1,
        "maximum": 730,
        "default": 365,
        "type": "number"
      }
    }
  },
  "auth_schema": {
    "methods": [
      {
        "type": "none"
      }
    ]
  }
}
